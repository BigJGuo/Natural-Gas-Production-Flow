requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pandas>=2.2
openpyxl>=3.1
click>=8.1
PyYAML>=6.0
python-dateutil>=2.9
playwright>=1.40   # Required for TC eConnects (CGT/Columbia Gulf/ANR). Run `python -m playwright install chromium` once.
dash>=2.17
dash-bootstrap-components>=1.6
websockets>=12          # For aisstream.io WebSocket (optional, only if you set AISSTREAM_API_KEY)
